# Publication gates

## Gate 1 — entrant and licence

- Public product attribution is approved as `Actalume by Krēˈādiv Worx`.
- MIT is approved and included as root `LICENSE` under the Krēˈādiv Worx brand.
- The Git repository uses the connected GitHub account's noreply address; no personal/custom email is exposed in commit metadata.

## Gate 2 — public repository

- Completed 2026-08-30 at `https://github.com/blackstackdev/actalume`.
- Public tree excludes local `memory/`, `release/`, `docs/design/` and `design-qa.md`.
- Git history uses the approved brand name and GitHub noreply identity.
- GitHub detects the root MIT licence and renders the README and public evidence images.

## Gate 3 — live deployment

- Completed 2026-08-30 through GitHub Pages at `https://blackstackdev.github.io/actalume/`.
- GitHub Actions builds the tested source on `main` and deploys the `dist` artifact with the `/actalume/` project base.
- HTTP 200, version, branded assets, persistence, desktop/mobile layout and zero public-origin console warnings/errors were verified.
- Supported Chrome with `WebMCP for testing` discovered all six native tools on the public origin; the same-origin verifier completed only `list_projects`, `inspect_project_contract` and `inspect_audit_history`.
- Keep the URL free and accessible through judging.

## Gate 4 — video

- Local production is complete: the locked build was captured at 1920×1080 and assembled with the approved narration and captions.
- The 119.800-second H.264/AAC candidate passed decode, colour, loudness and nine-frame visual QA.
- Upload publicly to YouTube with no copyrighted music or unlicensed third-party marks.

## Gate 5 — Devpost

- Join the WebMCP Challenge.
- Create the project using `DEVPOST-COPY.md`.
- Add the live URL, public repository and YouTube URL.
- Confirm English copy, technology tags, media ordering and entrant identity.
- Submit before 2026-09-03 22:00 SAST.
- Capture the submitted confirmation and project URL.

No gate authorises comments, DMs, account-profile changes, billing or spend.
