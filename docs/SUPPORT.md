# Support and recovery

- Use **Export Actalume ledger** before experimenting with imported data.
- Use **Reset demo** to restore the fictional seed state.
- Invalid imports are rejected without replacing the current ledger.
- If WebMCP is unavailable, the status reads **Demo mode** and the interface remains fully usable.
